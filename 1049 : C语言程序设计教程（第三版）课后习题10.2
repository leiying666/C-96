#include<stdio.h>
#include<string.h>
int main()
{
    char a[3][10] = { {0},{0},{0} };
    int i;
    for (i = 0; i < 3; i++)
    {
        gets(a[i]);
    }
    int tmp1 = strcmp(a[0], a[1]);
    int tmp2 = strcmp(a[1], a[2]);
    int tmp3= strcmp(a[0], a[2]);
    if (tmp1 > 0 && tmp2>0)
    {
        printf("%s\n%s\n%s", a[2], a[1], a[0]);
    }
    else if (tmp1 > 0 && tmp2 < 0 && tmp3>0)
    {
        printf("%s\n%s\n%s", a[1], a[2], a[0]);
    }
    else if (tmp1 > 0 && tmp2 < 0 && tmp3<0)
    {
        printf("%s\n%s\n%s", a[1], a[0], a[2]);
    }
    else if (tmp1 < 0 && tmp2 > 0 && tmp3>0)
    {
        printf("%s\n%s\n%s", a[2], a[0], a[1]);
    }
    else if (tmp1 < 0 && tmp2 < 0)
    {
        printf("%s\n%s\n%s", a[0], a[1], a[2]);
    }
    else
    {
        printf("%s\n%s\n%s", a[0], a[2], a[1]);
    }
    return 0;
}
